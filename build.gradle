plugins {
    id 'java'
}

group 'IPL'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    compile(files("/home/user/IdeaProjects/csvRaeder/out/artifacts/csvRaeder_main_jar/csvRaeder.main.jar"))
}
